apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hermes-agent
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/UriZafrir/home-lab-cluster
      targetRevision: ubuntu
      path: "extras/hermes-agent-helm-chart"
      helm:
        releaseName: hermes-agent
        valuesObject:
          image:
            tag: v2026.6.5
          service:
            enabled: true
            type: LoadBalancer
          apiServer:
            enabled: true
          externalSecret:
            enabled: true
            refreshInterval: 1h
            secretStoreRef:
              kind: ClusterSecretStore
              name: openbao-store
            target:
              name: openrouter-secret-k8s
            data:
              - secretKey: OPENROUTER_API_KEY
                remoteRef:
                  key: OPENROUTER_API_KEY
                  property: api_key
            # https://github.com/ultraworkers/hermes-agent-helm-chart/issues/1
            podSecurityContext:
              runAsUser: 0
              runAsNonRoot: false
            securityContext:
              allowPrivilegeEscalation: true
              readOnlyRootFilesystem: false
              runAsNonRoot: false
              capabilities:
                drop: []
            env:
              HERMES_UID: "1000"
              HERMES_GID: "1000"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: hermes-agent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
