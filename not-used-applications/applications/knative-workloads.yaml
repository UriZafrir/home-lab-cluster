apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: knative-workloads
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/UriZafrir/home-lab-cluster.git
      targetRevision: ubuntu
      path: extras/knative-workloads/knative-eventing-blockchain-demo
  destination:
    server: "https://kubernetes.default.svc"
    namespace: knative-workloads
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
