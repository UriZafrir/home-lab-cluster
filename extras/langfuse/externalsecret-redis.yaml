apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: langfuse-redis
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: SecretStore
  target:
    name: langfuse-redis  # Regular K8s Secret created here
  dataFrom:
    - extract:
        key: langfuse-redis