apiVersion: clickhouse.com/v1alpha1
kind: ClickHouseCluster
metadata:
  name: clickhouse
spec:
  replicas: 1
  dataVolumeClaimSpec:
    resources:
      requests:
        storage: 1Gi
  # keeperClusterRef:
  #   name: sample