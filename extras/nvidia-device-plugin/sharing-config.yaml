apiVersion: v1
kind: ConfigMap
metadata:
  name: time-slicing-config
data:
  sharing-config.yaml: |
    version: v1
    sharing:
      timeSlicing:
        resources:
          - name: nvidia.com/gpu
            replicas: 4
