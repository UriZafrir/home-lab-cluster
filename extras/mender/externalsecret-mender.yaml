apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seeweedfs-mender-creds
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: SecretStore
  target:
    name: seeweedfs-mender-creds
  dataFrom:
  - extract:
      key: seeweedfs-mender-creds