apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: exa-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: SecretStore
  target:
    name: exa-credentials   # Regular K8s Secret created here
  data:
  - secretKey: api-key
    remoteRef:
      key: exa-credentials
      property: password