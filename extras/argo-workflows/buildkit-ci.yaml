# apiVersion: argoproj.io/v1alpha1
# kind: Workflow
# metadata:
#   name: buildkit-ci
# spec:
#   entrypoint: buildkit-pipeline
#   arguments:
#     parameters:
#     - name: git-repo
#       value: "https://github.com/komljen/dockerfile-examples"
#     - name: image
#       value: "yourreg/app:latest"
#   templates:
#   - name: buildkit-pipeline
#     dag:
#       tasks:
#       - name: clone                # ✅ Fixed: No arguments needed
#         template: git-clone        # Git-clone uses workflow.parameters directly
#   - name: git-clone
#     container:
#       image: alpine/git
#       command: [sh, -c]
#       args: ["git clone {{workflow.parameters.git-repo}} /workspace/src"]
#       volumeMounts:
#       - name: workspace
#         mountPath: /workspace
#   volumeClaimTemplates:           # ✅ Move volumes UP (before templates end)
#   - metadata:
#       name: workspace
#     spec:
#       accessModes: ["ReadWriteOnce"]
#       resources:
#         requests:
#           storage: 5Gi
